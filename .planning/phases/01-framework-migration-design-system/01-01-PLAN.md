---
phase: 01-framework-migration-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - astro.config.mjs
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/BaseHead.astro
  - src/env.d.ts
  - src/pages/index.astro
  - biome.json
  - .gitignore
autonomous: true
requirements:
  - FRMK-01
  - FRMK-03
  - FRMK-06

must_haves:
  truths:
    - "Astro project builds successfully with `npm run build`"
    - "Tailwind v4 utility classes render in built HTML output"
    - "shadcn/ui Button component is available for import"
    - "Inter font is loaded and configured as the default sans-serif"
    - "Old Hugo files (layouts/, config.yaml, data/) are untouched"
  artifacts:
    - path: "package.json"
      provides: "Astro + Tailwind + React + shadcn dependencies"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro config with Tailwind vite plugin and React integration"
      contains: "tailwindcss"
    - path: "src/styles/global.css"
      provides: "Tailwind v4 @theme with dark/light color tokens"
      contains: "@theme"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Base HTML layout shell"
      contains: "BaseHead"
    - path: "src/components/BaseHead.astro"
      provides: "HTML head with meta tags and font import"
      contains: "fontsource"
  key_links:
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "CSS import"
      pattern: "global\\.css"
---

<objective>
Scaffold the Astro 5.x project with Tailwind CSS v4 design system, React integration, shadcn/ui, and the base layout skeleton.

Purpose: Establish the foundational project structure that all subsequent plans build upon -- Astro framework, build pipeline, design tokens, and base layout.
Output: A building Astro project with Tailwind v4 theme tokens, shadcn/ui initialized, BaseLayout and BaseHead components, and a placeholder index page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-framework-migration-design-system/01-CONTEXT.md
@.planning/phases/01-framework-migration-design-system/01-RESEARCH.md
@.planning/phases/01-framework-migration-design-system/01-UI-SPEC.md
@.planning/phases/01-framework-migration-design-system/01-VALIDATION.md
@config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro project with React, Tailwind v4, and shadcn/ui</name>
  <files>package.json, tsconfig.json, astro.config.mjs, biome.json, .gitignore, src/env.d.ts</files>
  <read_first>
    - /Users/yongseokjo/projects/my-site/.gitignore
    - /Users/yongseokjo/projects/my-site/.planning/phases/01-framework-migration-design-system/01-RESEARCH.md (Installation section, Pitfall 6 shadcn init order)
  </read_first>
  <action>
1. Create the Astro project in a temp directory to avoid conflicts with existing Hugo files (per D-17 keep old files, per Research Open Question 1):
   ```bash
   cd /tmp && npm create astro@latest astro-scaffold -- --template minimal --install --no-git --typescript strict
   ```
2. Copy these files from /tmp/astro-scaffold into the project root: `package.json`, `tsconfig.json`, `astro.config.mjs`, `src/env.d.ts`. Create `src/` directory if it does not exist.
3. Install core dependencies in the project root:
   ```bash
   npx astro add react mdx netlify --yes
   npm install tailwindcss @tailwindcss/vite
   npm install @fontsource-variable/inter
   npm install -D @biomejs/biome @astrojs/check typescript
   ```
4. Configure `astro.config.mjs` exactly as:
   ```javascript
   import { defineConfig } from "astro/config";
   import react from "@astrojs/react";
   import mdx from "@astrojs/mdx";
   import netlify from "@astrojs/netlify";
   import tailwindcss from "@tailwindcss/vite";

   export default defineConfig({
     site: "https://yongseokjo.com",
     output: "static",
     adapter: netlify(),
     integrations: [react(), mdx()],
     vite: {
       plugins: [tailwindcss()],
     },
   });
   ```
5. Ensure `tsconfig.json` has the `@/*` path alias for shadcn:
   ```json
   {
     "extends": "astro/tsconfigs/strict",
     "compilerOptions": {
       "baseUrl": ".",
       "paths": {
         "@/*": ["./src/*"]
       },
       "jsx": "react-jsx",
       "jsxImportSource": "react"
     }
   }
   ```
6. Initialize shadcn/ui (AFTER React + Tailwind are configured per Pitfall 6):
   ```bash
   npx shadcn@latest init --defaults
   npx shadcn@latest add button
   ```
   If interactive prompts appear, select: style=new-york, base-color=neutral, css-variables=yes.
7. Initialize Biome:
   ```bash
   npx @biomejs/biome init
   ```
8. Update `.gitignore` to append (do not replace existing entries):
   ```
   node_modules/
   dist/
   .astro/
   .env
   .env.*
   ```
9. Clean up temp scaffold: `rm -rf /tmp/astro-scaffold`
  </action>
  <verify>
    <automated>cd /Users/yongseokjo/projects/my-site && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"astro"` as a dependency
    - `package.json` contains `"tailwindcss"` as a dependency
    - `package.json` contains `"@tailwindcss/vite"` as a dependency
    - `package.json` contains `"react"` as a dependency
    - `package.json` contains `"@astrojs/react"` as a dependency
    - `package.json` contains `"@astrojs/netlify"` as a dependency
    - `package.json` contains `"@fontsource-variable/inter"` as a dependency
    - `astro.config.mjs` contains `tailwindcss()` in vite plugins
    - `astro.config.mjs` contains `output: "static"`
    - `astro.config.mjs` contains `site: "https://yongseokjo.com"`
    - `tsconfig.json` contains `"@/*"` path alias
    - `src/components/ui/button.tsx` exists (shadcn Button component)
    - `.gitignore` contains `node_modules/` and `dist/` and `.astro/`
    - Old Hugo files still exist: `layouts/` directory, `config.yaml`, `data/` directory
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Astro project scaffolded with React, Tailwind v4, shadcn/ui Button, Biome. Build succeeds. Hugo files untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create design system (global.css theme) and BaseLayout + BaseHead components</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro, src/components/BaseHead.astro, src/pages/index.astro</files>
  <read_first>
    - /Users/yongseokjo/projects/my-site/.planning/phases/01-framework-migration-design-system/01-UI-SPEC.md (Color section, Typography section, Spacing section, Tailwind v4 CSS Theme Variables section)
    - /Users/yongseokjo/projects/my-site/.planning/phases/01-framework-migration-design-system/01-RESEARCH.md (Pattern 1: Tailwind v4 CSS-Only Configuration, BaseLayout Component code example)
    - /Users/yongseokjo/projects/my-site/astro.config.mjs
  </read_first>
  <action>
1. Create `src/styles/global.css` with the exact Tailwind v4 theme from UI-SPEC (per D-01, D-02, D-04, D-12, D-13):
   ```css
   @import "tailwindcss";

   @theme {
     --font-sans: "Inter Variable", "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
     --color-background: #0a0e1a;
     --color-foreground: #e2e8f0;
     --color-card: #141b2d;
     --color-card-foreground: #e2e8f0;
     --color-primary: #d4a54a;
     --color-primary-foreground: #0a0e1a;
     --color-secondary: #1a2236;
     --color-secondary-foreground: #e2e8f0;
     --color-muted: #1a2236;
     --color-muted-foreground: #94a3b8;
     --color-accent: #d4a54a;
     --color-accent-foreground: #0a0e1a;
     --color-destructive: #ef4444;
     --color-border: #1e293b;
     --color-ring: #d4a54a;
     --radius-lg: 1rem;
     --radius-md: 0.75rem;
     --radius-sm: 0.5rem;
   }

   .light {
     --color-background: #ffffff;
     --color-foreground: #0f172a;
     --color-card: #f8fafc;
     --color-card-foreground: #0f172a;
     --color-primary: #b8860b;
     --color-primary-foreground: #ffffff;
     --color-secondary: #f1f5f9;
     --color-secondary-foreground: #0f172a;
     --color-muted: #f1f5f9;
     --color-muted-foreground: #64748b;
     --color-accent: #b8860b;
     --color-accent-foreground: #ffffff;
     --color-destructive: #dc2626;
     --color-border: #e2e8f0;
     --color-ring: #b8860b;
   }
   ```
2. Create `src/components/BaseHead.astro` (per D-03, FRMK-06):
   ```astro
   ---
   import "@fontsource-variable/inter";

   interface Props {
     title: string;
     description?: string;
   }

   const { title, description = "Yongseok Jo - Computational Astrophysicist" } = Astro.props;
   ---
   <meta charset="utf-8" />
   <meta name="viewport" content="width=device-width, initial-scale=1" />
   <meta name="generator" content={Astro.generator} />
   <meta name="description" content={description} />
   <title>{title} | Yongseok Jo</title>
   <link rel="icon" type="image/x-icon" href="/favicon.ico" />
   ```
3. Create `src/layouts/BaseLayout.astro` (per D-08 narrow prose-width 700px max, Research BaseLayout example):
   ```astro
   ---
   import "../styles/global.css";
   import BaseHead from "../components/BaseHead.astro";

   interface Props {
     title: string;
     description?: string;
   }

   const { title, description } = Astro.props;
   ---
   <!doctype html>
   <html lang="en" class="dark">
     <head>
       <BaseHead title={title} description={description} />
     </head>
     <body class="bg-background text-foreground min-h-screen flex flex-col font-sans">
       <main class="flex-1 w-full max-w-[700px] mx-auto px-4 md:px-6 lg:px-8 py-16">
         <slot />
       </main>
     </body>
   </html>
   ```
   Note: Navigation, Footer, and ThemeScript are added in Plan 02. This skeleton enables build verification.
4. Create `src/pages/index.astro` as a minimal placeholder that uses BaseLayout:
   ```astro
   ---
   import BaseLayout from "../layouts/BaseLayout.astro";
   ---
   <BaseLayout title="Home">
     <h1 class="text-4xl font-bold text-primary mb-4">Yongseok Jo</h1>
     <p class="text-muted-foreground">Computational Astrophysicist</p>
     <p class="text-muted-foreground mt-4">Site Under Construction</p>
     <p class="text-sm text-muted-foreground">This site is being rebuilt with a new design. Check back soon.</p>
   </BaseLayout>
   ```
5. Copy static assets from Hugo to Astro public directory:
   ```bash
   mkdir -p src/pages public/icons public/images
   cp static/icons/* public/icons/ 2>/dev/null || true
   cp static/images/* public/images/ 2>/dev/null || true
   cp static/robots.txt public/robots.txt 2>/dev/null || true
   cp static/icons/favicon.ico public/favicon.ico 2>/dev/null || true
   ```
  </action>
  <verify>
    <automated>cd /Users/yongseokjo/projects/my-site && npm run build && grep -l "background" src/styles/global.css && grep -l "BaseHead" src/layouts/BaseLayout.astro && grep -l "BaseLayout" src/pages/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/global.css` contains `@import "tailwindcss"`
    - `src/styles/global.css` contains `@theme`
    - `src/styles/global.css` contains `--color-background: #0a0e1a`
    - `src/styles/global.css` contains `--color-primary: #d4a54a`
    - `src/styles/global.css` contains `--font-sans: "Inter Variable"`
    - `src/styles/global.css` contains `.light` class with `--color-background: #ffffff`
    - `src/layouts/BaseLayout.astro` contains `max-w-[700px]`
    - `src/layouts/BaseLayout.astro` contains `bg-background text-foreground`
    - `src/layouts/BaseLayout.astro` contains `font-sans`
    - `src/components/BaseHead.astro` contains `@fontsource-variable/inter`
    - `src/components/BaseHead.astro` contains `<meta name="viewport"`
    - `src/pages/index.astro` contains `import BaseLayout`
    - `src/pages/index.astro` contains `text-primary`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Design system CSS with dark/light tokens is active. BaseLayout renders with 700px prose width, Inter font, and dark mode default. Placeholder index page builds and renders with theme colors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> project | Third-party packages installed from npm |
| Static output -> CDN | Built HTML/CSS/JS served to public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm dependencies | mitigate | Use exact versions in package-lock.json; run `npm audit` after install; commit lockfile |
| T-01-02 | Information Disclosure | .env files | mitigate | Ensure `.env` and `.env.*` are in `.gitignore` (no secrets in Phase 1 but establish pattern) |
| T-01-03 | Tampering | Markdown content XSS | accept | Astro's Markdown renderer auto-escapes HTML by default; no user input in Phase 1 |
</threat_model>

<verification>
- `npm run build` exits with code 0
- `npx astro check` reports no errors
- Built output in `dist/` contains HTML with Tailwind utility classes
- `layouts/`, `config.yaml`, `data/` directories still exist (Hugo files preserved per D-17)
</verification>

<success_criteria>
- Astro 5.x project builds successfully
- Tailwind v4 design tokens (dark space theme + light academic theme) are configured
- shadcn/ui Button component is installed and importable
- Inter font is loaded via fontsource
- BaseLayout provides 700px max-width content area
- Placeholder index page renders with theme colors
- Hugo files are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/01-framework-migration-design-system/01-01-SUMMARY.md`
</output>
