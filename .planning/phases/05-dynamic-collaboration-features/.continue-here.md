# Continue Here — Post-Deployment Polish

**Paused:** 2026-04-13 20:19
**Phase:** All 5 phases complete. Post-completion polish in progress.
**State:** Site deployed to Netlify, code on GitHub at `YongseokJo/my-site`.

## Current Position

All 5 phases (Framework, Academic Content, Product Pages & SEO, Backend + Auth, Dynamic Collaboration) are complete and verified. Site is deployed on Netlify connected to GitHub `main`.

## Outstanding Issues

### 1. Contact form not working on Netlify (LIVE issue)
- **File:** `src/pages/contact.astro`, `src/islands/ContactForm.tsx`
- **Setup:** Uses Netlify Forms pattern — hidden static HTML form + React island POSTs to `/` with `form-name=contact`
- **Likely cause:** Middleware is SSR on all auth-aware routes. The contact page may not be getting prerendered as static HTML, so Netlify's build-time form parser can't detect the hidden form
- **User tested:** On Netlify deployed site, submission does not work
- **Recommended fix:** Switch Contact form to Formspree (same pattern as BetaSignupForm/FeedbackForm). User already has Formspree account — needs a third form ID
- **Alternative:** Add `export const prerender = true;` to `src/pages/contact.astro` if middleware allows

### 2. Resend domain verification
- **From address:** `me@yongseokjo.com` (in `src/pages/api/proposals/notify-approval.ts`)
- **Issue:** Resend requires domain verification for `yongseokjo.com` — user needs to add DNS records
- **Fallback:** `onboarding@resend.dev` works for testing
- **User action needed:** Verify domain at https://resend.com/domains

## Environment Variables

**In Netlify dashboard** (already added by user):
- `PUBLIC_SUPABASE_URL`
- `PUBLIC_SUPABASE_ANON_KEY`
- `RESEND_API_KEY`

**In GitHub repo secrets** (for keep-alive cron):
- `SUPABASE_URL` (without PUBLIC_ prefix)
- `SUPABASE_ANON_KEY` (without PUBLIC_ prefix)

## Supabase Migrations Applied

All migrations 00001-00010 were run in the Supabase SQL Editor:
- 00001-00004: Initial schema + RLS (profiles, issues, proposals)
- 00005: proposal_reviews (review_comment, reviewer columns)
- 00006: add co_admin role
- 00007: rename viewer to researcher
- 00008: delete policies for issues and proposals
- 00009: pi_email, mentor_email columns
- 00010: affiliation column

## Recent Polish Done Post-Phase 5

- Profile photo: `public/images/YongseokJo.jpg`
- Readea screenshots: side-by-side, alternating color schemes, click-to-zoom
- Publications: auto-fetched from ADS public library (`miCGF6FnSgW_IUWliF3sDw`), `node scripts/fetch_publications.mjs`
- Research: 4 cards (Physics-Driven ML second, Nuclear Star Cluster paper added)
- Enzo-Abyss: features updated (Enzo, Direct N-body, Star-by-Star, Robust BH Physics), Quick Start removed, docs "To be updated"
- Navigation: Enzo-Abyss + Readea moved to end, highlighted with gold border, dividers added
- Dashboard: collapsible panels, edit + delete buttons on My Issues / My Proposals
- UserListPanel at bottom of admin dashboard
- Role renamed: Viewer → Researcher (both in UI and DB)
- Forms: added label/input spacing (`space-y-2` / `space-y-6`)
- Issues renamed to "To-Do / Issues" everywhere
- Proposal form: PI + PI Email side by side, Mentor + Mentor Email side by side, Position + Affiliation side by side, Basic Profile optional
- Email notification on proposal approval (Resend via `/api/proposals/notify-approval`)

## Active TODOs

- [ ] Fix Contact form on Netlify (switch to Formspree recommended)
- [ ] Verify `yongseokjo.com` domain in Resend for `me@yongseokjo.com` sender
- [ ] Test proposal approval email flow end-to-end after domain verification

## Key Files To Reference On Resume

- `src/islands/ContactForm.tsx` — current Netlify Forms implementation
- `src/islands/BetaSignupForm.tsx` — reference Formspree pattern
- `src/pages/api/proposals/notify-approval.ts` — Resend email endpoint
- `scripts/fetch_publications.mjs` — ADS library publication sync

## Resume Command

```
/gsd-resume-work
```

Or just ask to "continue fixing the contact form" or "set up Resend domain verification".
