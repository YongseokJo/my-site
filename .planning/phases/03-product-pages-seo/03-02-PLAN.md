---
phase: 03-product-pages-seo
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/software.astro
  - src/pages/software/install.astro
  - src/pages/software/usage.astro
autonomous: true
requirements:
  - SIM-01
  - SIM-02
  - SIM-03
must_haves:
  truths:
    - "Visitor sees Enzo-Abyss product name and tagline on /software"
    - "Visitor sees feature highlights describing key simulation capabilities"
    - "Visitor sees a quick-start guide with code snippets"
    - "Visitor can navigate to /software/install and /software/usage sub-pages"
    - "Documentation sub-pages have breadcrumb navigation back to /software"
  artifacts:
    - path: "src/pages/software.astro"
      provides: "Enzo-Abyss landing page with product header, features, quick-start, doc links"
      contains: "Enzo-Abyss"
    - path: "src/pages/software/install.astro"
      provides: "Installation documentation page"
      contains: "Installation"
    - path: "src/pages/software/usage.astro"
      provides: "Usage guide documentation page"
      contains: "Usage Guide"
  key_links:
    - from: "src/pages/software.astro"
      to: "/software/install"
      via: "anchor link"
      pattern: "href.*software/install"
    - from: "src/pages/software/install.astro"
      to: "/software"
      via: "Breadcrumb component"
      pattern: "Breadcrumb"
---

<objective>
Build the Enzo-Abyss simulation package landing page and documentation sub-pages, replacing the current placeholder at /software.

Purpose: Give visitors a complete product page for the Enzo-Abyss cosmological simulation framework with features, quick-start guide, and documentation.
Output: Three pages — /software (landing), /software/install, /software/usage

Note on SIM-03: Requirement SIM-03 (contributor list) is explicitly excluded from Phase 3 per user decision D-07. No contributor list or GitHub link will be implemented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-product-pages-seo/03-CONTEXT.md
@.planning/phases/03-product-pages-seo/03-RESEARCH.md
@.planning/phases/03-product-pages-seo/03-UI-SPEC.md
@.planning/phases/03-product-pages-seo/03-01-SUMMARY.md

<interfaces>
<!-- Components created in Plan 01 that this plan uses -->

From src/components/ProductHeader.astro:
```astro
interface Props {
  name: string;
  tagline: string;
}
```

From src/components/FeatureGrid.astro:
```astro
interface Props {
  sectionTitle?: string;
}
<!-- Uses <slot /> for FeatureCard children -->
```

From src/components/FeatureCard.astro:
```astro
interface Props {
  icon: string;
  title: string;
  description: string;
}
<!-- Uses <slot /> for icon content -->
```

From src/components/CodeBlock.astro:
```astro
interface Props {
  code: string;
  language?: string;
}
```

From src/components/Breadcrumb.astro:
```astro
interface Props {
  items: { label: string; href?: string }[];
}
```

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
<!-- Now uses SEOHead internally — OG tags automatic -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Enzo-Abyss landing page at /software</name>
  <files>src/pages/software.astro</files>
  <read_first>src/pages/software.astro, src/components/ProductHeader.astro, src/components/FeatureGrid.astro, src/components/FeatureCard.astro, src/components/CodeBlock.astro, src/layouts/BaseLayout.astro, .planning/phases/03-product-pages-seo/03-UI-SPEC.md</read_first>
  <action>
Replace the placeholder content in `src/pages/software.astro` with the full Enzo-Abyss landing page per D-01 through D-05 and the UI-SPEC Enzo-Abyss layout.

Use `BaseLayout` with title="Enzo-Abyss" and description="Enzo-Abyss: a cosmological simulation framework for studying the formation of first star clusters and black holes in the early universe." (per UI-SPEC SEO defaults).

Page structure (per UI-SPEC "Enzo-Abyss Landing" layout):

**1. ProductHeader** (per D-02, D-03):
```astro
<ProductHeader name="Enzo-Abyss" tagline="Cosmological simulation framework for studying first star clusters and black holes" />
```

**2. FeatureGrid** with section title "Features" (per D-04):
Create 4-6 FeatureCards highlighting key Enzo-Abyss capabilities. Use placeholder but realistic feature content:
- "Adaptive Mesh Refinement" — "Multi-level grid refinement for resolving small-scale structure within large cosmological volumes."
- "N-body + Hydrodynamics" — "Self-consistent hybrid approach coupling gravitational N-body dynamics with hydrodynamic gas evolution."
- "Star Formation & Feedback" — "Sub-grid models for star formation, supernova feedback, and chemical enrichment in early universe environments."
- "Black Hole Physics" — "Formation and growth of massive black holes with accretion and feedback processes in primordial halos."
- "Scalable Performance" — "Optimized for high-performance computing clusters with efficient parallel decomposition."
- "Analysis Toolkit" — "Built-in tools for halo finding, merger tree construction, and visualization of simulation outputs."

For each FeatureCard, pass an inline SVG icon (from Lucide icon set). Use appropriate Lucide icons: Layers for AMR, Orbit for N-body, Sparkles for Star Formation, Circle for Black Hole, Zap for Performance, BarChart3 for Analysis. Render icons as inline SVG in the slot with `class="size-6 text-primary" aria-hidden="true"`.

**3. Quick Start section** (per D-05):
Section heading: `<h2 class="text-2xl font-bold mb-6 mt-12">Quick Start</h2>`

3-4 numbered steps with CodeBlock components showing installation and first run:
- Step 1: "Clone the repository" with code `git clone https://github.com/enzo-project/enzo-abyss.git`
- Step 2: "Configure and build" with code showing `cd enzo-abyss && ./configure && make`
- Step 3: "Run a test simulation" with code `./bin/enzo TestProblem.enzo`

Each step: `<div class="mb-6"><p class="text-base mb-3 font-medium">Step N: Title</p><CodeBlock code="..." language="bash" /></div>`

End with a "Get Started" button linking to /software/install:
```astro
<a href="/software/install" class="inline-flex items-center justify-center px-6 py-3 bg-primary text-primary-foreground font-medium rounded-lg hover:opacity-90 transition-opacity mt-4">Get Started</a>
```

**4. Documentation links section** (per UI-SPEC):
Section heading: `<h2 class="text-2xl font-bold mb-6 mt-12">Documentation</h2>`
Two links as simple list items:
- "Installation" linking to /software/install
- "Usage Guide" linking to /software/usage
Each link: `<a href="..." class="flex items-center gap-2 text-base text-primary hover:underline transition-all duration-150 py-2">` with a right-arrow SVG icon (Lucide ArrowRight as inline SVG).

No contributor list or GitHub link per D-07.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/software.astro` contains `ProductHeader` with `name="Enzo-Abyss"`
    - `src/pages/software.astro` contains `tagline="Cosmological simulation framework for studying first star clusters and black holes"` (D-03)
    - `src/pages/software.astro` contains `FeatureGrid` and at least 4 `FeatureCard` components
    - `src/pages/software.astro` contains `Quick Start` heading and at least 2 `CodeBlock` components
    - `src/pages/software.astro` contains `href="/software/install"` link
    - `src/pages/software.astro` contains `href="/software/usage"` link
    - `src/pages/software.astro` does NOT contain "contributor" or "GitHub" (per D-07)
    - `src/pages/software.astro` contains `description=` with meta description text
    - Build succeeds
  </acceptance_criteria>
  <done>Enzo-Abyss landing page at /software shows product header with tagline, 4-6 feature cards, quick-start guide with code blocks, and documentation links</done>
</task>

<task type="auto">
  <name>Task 2: Create Enzo-Abyss documentation sub-pages</name>
  <files>src/pages/software/install.astro, src/pages/software/usage.astro</files>
  <read_first>src/components/Breadcrumb.astro, src/components/CodeBlock.astro, src/layouts/BaseLayout.astro, .planning/phases/03-product-pages-seo/03-UI-SPEC.md</read_first>
  <action>
Create the `src/pages/software/` directory and two documentation sub-pages per D-06 and the UI-SPEC "Enzo-Abyss Documentation Sub-pages" layout.

**1. src/pages/software/install.astro** — Installation documentation:

Use `BaseLayout` with title="Installation" and description="How to install and set up Enzo-Abyss cosmological simulation framework."

Page structure:
- Breadcrumb: `<Breadcrumb items={[{ label: "Software", href: "/software" }, { label: "Installation" }]} />`
- Page heading: `<h1 class="text-2xl font-bold mb-8">Installation</h1>`
- Documentation prose in `<div class="prose-like space-y-6">`:
  - "Prerequisites" section (h2) listing system requirements: C/C++ compiler, MPI library, HDF5, Python 3 for analysis scripts
  - "Download" section (h2) with `CodeBlock` showing git clone command
  - "Build from Source" section (h2) with step-by-step `CodeBlock` components showing configure + make
  - "Verify Installation" section (h2) with a test command `CodeBlock`
  - Use realistic but placeholder content appropriate for a cosmological simulation framework
  - Each h2: `class="text-xl font-bold mt-8 mb-4"`
  - Body paragraphs: `class="text-base text-foreground"`

**2. src/pages/software/usage.astro** — Usage guide:

Use `BaseLayout` with title="Usage Guide" and description="How to configure and run simulations with Enzo-Abyss."

Page structure:
- Breadcrumb: `<Breadcrumb items={[{ label: "Software", href: "/software" }, { label: "Usage Guide" }]} />`
- Page heading: `<h1 class="text-2xl font-bold mb-8">Usage Guide</h1>`
- Documentation prose:
  - "Configuration" section (h2) — how to set up a simulation parameter file, with CodeBlock showing an example .enzo config snippet
  - "Running a Simulation" section (h2) — command to launch with MPI, CodeBlock with `mpirun` command
  - "Output Files" section (h2) — description of output format (HDF5 datasets), how to find them
  - "Analysis" section (h2) — brief mention of yt (Python analysis toolkit) for post-processing, with a CodeBlock showing a simple yt script
  - Same heading/body styling as install page
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/software/install.astro` exists and contains `Breadcrumb` with `label: "Software"` and `href: "/software"`
    - `src/pages/software/install.astro` contains `<h1` with "Installation"
    - `src/pages/software/install.astro` contains at least 2 `CodeBlock` components
    - `src/pages/software/usage.astro` exists and contains `Breadcrumb` with `label: "Software"` and `href: "/software"`
    - `src/pages/software/usage.astro` contains `<h1` with "Usage Guide"
    - `src/pages/software/usage.astro` contains at least 2 `CodeBlock` components
    - Both pages use `BaseLayout`
    - Build succeeds and pages are accessible at /software/install and /software/usage
  </acceptance_criteria>
  <done>Installation and Usage Guide sub-pages created with breadcrumb navigation, code blocks, and documentation prose, accessible via /software/install and /software/usage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All static pages with no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | Code examples | accept | Code examples are public documentation; no secrets |
</threat_model>

<verification>
1. `npm run build` passes
2. `/software` page shows Enzo-Abyss product header, features, quick-start, and doc links
3. `/software/install` and `/software/usage` pages render with breadcrumb navigation
4. No contributor list or GitHub link appears (D-07)
</verification>

<success_criteria>
- /software replaces placeholder with full Enzo-Abyss landing (product header per D-02/D-03, features per D-04, quick-start per D-05)
- /software/install and /software/usage exist as separate sub-pages per D-06
- All three pages have breadcrumb or cross-links for navigation
- No contributor list or GitHub link per D-07
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-pages-seo/03-02-SUMMARY.md`
</output>
