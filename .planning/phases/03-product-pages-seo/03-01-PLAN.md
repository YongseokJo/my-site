---
phase: 03-product-pages-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/SEOHead.astro
  - src/components/Breadcrumb.astro
  - src/components/CodeBlock.astro
  - src/components/FeatureCard.astro
  - src/components/FeatureGrid.astro
  - src/components/ProductHeader.astro
  - src/components/DeviceMockupPlaceholder.astro
  - src/components/ReadeaHero.astro
  - src/components/TutorialStep.astro
  - src/components/TutorialNav.astro
  - src/layouts/BaseLayout.astro
  - src/layouts/HomeLayout.astro
  - src/components/ui/select.tsx
  - src/components/ui/dialog.tsx
  - astro.config.mjs
autonomous: true
requirements:
  - SEO-01
must_haves:
  truths:
    - "All pages automatically get OpenGraph meta tags via SEOHead"
    - "Shared components exist for downstream plans to use without defining their own"
    - "astro-pagefind integration is configured in Astro config"
  artifacts:
    - path: "src/components/SEOHead.astro"
      provides: "OpenGraph + Twitter Card + canonical URL meta tags"
      contains: "og:title"
    - path: "src/components/Breadcrumb.astro"
      provides: "Breadcrumb navigation with aria-label"
      contains: "aria-label"
    - path: "src/components/CodeBlock.astro"
      provides: "Styled code block with copy button"
      contains: "Copy code to clipboard"
    - path: "src/components/FeatureCard.astro"
      provides: "Icon + title + description card"
    - path: "src/components/FeatureGrid.astro"
      provides: "2-col responsive grid of feature cards"
    - path: "src/components/ProductHeader.astro"
      provides: "Product name + tagline block"
    - path: "src/components/DeviceMockupPlaceholder.astro"
      provides: "Placeholder image with 'Screenshot coming soon' text"
    - path: "src/components/ReadeaHero.astro"
      provides: "Full-width hero with gradient overlay for Readea"
    - path: "astro.config.mjs"
      provides: "astro-pagefind integration configured"
      contains: "pagefind"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/SEOHead.astro"
      via: "import and render"
      pattern: "SEOHead"
    - from: "src/layouts/HomeLayout.astro"
      to: "src/components/SEOHead.astro"
      via: "import and render"
      pattern: "SEOHead"
---

<objective>
Install new dependencies (astro-pagefind, shadcn select/dialog), create all shared Astro components used across Phase 3 product pages, and replace BaseHead with SEOHead in both layouts so all pages automatically get OpenGraph/Twitter Card meta tags.

Purpose: Establish the component foundation and SEO infrastructure so that Plans 02-05 can focus on page-level work without building shared primitives.
Output: Shared components, updated layouts with SEOHead, astro-pagefind configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-pages-seo/03-CONTEXT.md
@.planning/phases/03-product-pages-seo/03-RESEARCH.md
@.planning/phases/03-product-pages-seo/03-UI-SPEC.md
@src/layouts/BaseLayout.astro
@src/layouts/HomeLayout.astro
@src/components/BaseHead.astro
@src/styles/global.css

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/components/BaseHead.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
```

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
```

From src/layouts/HomeLayout.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
<!-- Uses slot name="hero" for full-width hero content -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, add shadcn components, configure astro-pagefind</name>
  <files>package.json, astro.config.mjs, src/components/ui/select.tsx, src/components/ui/dialog.tsx</files>
  <read_first>package.json, astro.config.mjs</read_first>
  <action>
1. Install astro-pagefind:
   ```bash
   npm install astro-pagefind
   ```

2. Add shadcn Select and Dialog components:
   ```bash
   npx shadcn@latest add select dialog
   ```

3. Update `astro.config.mjs` to add the astro-pagefind integration. Import `pagefind` from `astro-pagefind` and add it to the `integrations` array after `mdx()`:
   ```javascript
   import pagefind from "astro-pagefind";
   // ...
   integrations: [react(), mdx(), pagefind()],
   ```

4. Verify with `npm run build` that the build succeeds with the new integration.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"astro-pagefind"` as a dependency
    - `astro.config.mjs` contains `import pagefind from "astro-pagefind"` and `pagefind()` in integrations array
    - `src/components/ui/select.tsx` exists
    - `src/components/ui/dialog.tsx` exists
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>astro-pagefind installed and configured, shadcn Select and Dialog components added, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create SEOHead component and replace BaseHead in both layouts</name>
  <files>src/components/SEOHead.astro, src/layouts/BaseLayout.astro, src/layouts/HomeLayout.astro</files>
  <read_first>src/components/BaseHead.astro, src/layouts/BaseLayout.astro, src/layouts/HomeLayout.astro, src/styles/global.css</read_first>
  <action>
1. Create `src/components/SEOHead.astro` as a drop-in replacement for BaseHead that adds OpenGraph and Twitter Card meta tags. Per the UI-SPEC SEOHead interaction contract and RESEARCH Pattern 3:

```astro
---
import "@fontsource-variable/inter";

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  ogType?: string;
  canonicalUrl?: string;
}
const {
  title,
  description = "Yongseok Jo - Computational Astrophysicist",
  ogImage = "/images/og-default.png",
  ogType = "website",
  canonicalUrl = Astro.url.href,
} = Astro.props;

const fullTitle = `${title} | Yongseok Jo`;
---
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<meta name="generator" content={Astro.generator} />
<meta name="description" content={description} />
<title>{fullTitle}</title>
<link rel="icon" type="image/x-icon" href="/favicon.ico" />
<link rel="canonical" href={canonicalUrl} />
<!-- OpenGraph -->
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:type" content={ogType} />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:image" content={ogImage} />
<!-- Twitter Card -->
<meta name="twitter:card" content="summary" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<slot />
```

2. Update `src/layouts/BaseLayout.astro`: Replace `import BaseHead` with `import SEOHead` from `../components/SEOHead.astro`. Replace `<BaseHead title={title} description={description} />` with `<SEOHead title={title} description={description} />`. Keep all other content identical.

3. Update `src/layouts/HomeLayout.astro`: Same replacement — `SEOHead` instead of `BaseHead`.

4. Do NOT delete `BaseHead.astro` — other pages may still reference it directly. The layouts now use SEOHead which is a superset.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SEOHead.astro` exists and contains `og:title`, `og:description`, `og:type`, `og:url`, `og:image`, `twitter:card`, `twitter:title`, `twitter:description`, `rel="canonical"`
    - `src/layouts/BaseLayout.astro` imports `SEOHead` (not `BaseHead`) and renders `<SEOHead`
    - `src/layouts/HomeLayout.astro` imports `SEOHead` (not `BaseHead`) and renders `<SEOHead`
    - Build succeeds with no errors
  </acceptance_criteria>
  <done>SEOHead component created with full OG/Twitter meta, both layouts updated to use it, all existing pages automatically get OG tags</done>
</task>

<task type="auto">
  <name>Task 3: Create shared Phase 3 Astro components</name>
  <files>src/components/Breadcrumb.astro, src/components/CodeBlock.astro, src/components/FeatureCard.astro, src/components/FeatureGrid.astro, src/components/ProductHeader.astro, src/components/DeviceMockupPlaceholder.astro, src/components/ReadeaHero.astro, src/components/TutorialStep.astro, src/components/TutorialNav.astro</files>
  <read_first>src/styles/global.css, src/components/BaseHead.astro, .planning/phases/03-product-pages-seo/03-UI-SPEC.md</read_first>
  <action>
Create each component following the UI-SPEC precisely. All use Tailwind v4 utility classes with the existing theme tokens (bg-card, text-foreground, text-muted-foreground, border-border, bg-primary, text-primary-foreground, bg-muted, etc.).

**1. Breadcrumb.astro** (UI-SPEC interaction contract #5):
- Props: `items: { label: string; href?: string }[]`
- Render `<nav aria-label="Breadcrumb">` with `<ol>` list
- Items separated by "/" (`aria-hidden="true"`)
- All segments except last are clickable links in `text-muted-foreground` with `hover:text-accent`
- Last segment: plain text in `text-foreground` with `aria-current="page"`
- Font: `text-sm text-muted-foreground`
- Transition: `transition-colors duration-150`

**2. CodeBlock.astro** (UI-SPEC interaction contract #3):
- Props: `code: string; language?: string`
- Container: `bg-muted border border-border rounded-lg relative` with padding `p-4`
- Code text: monospace font `font-mono text-sm`, line-height 1.6, `overflow-x-auto`
- Language label: top-left, `text-xs text-muted-foreground` if language prop provided
- Copy button: absolute top-right, Lucide Clipboard icon 16x16, `aria-label="Copy code to clipboard"`
- Copy behavior: inline `<script>` using `navigator.clipboard.writeText()`, icon changes to checkmark for 2 seconds then reverts
- The `<pre><code>` inner content renders the `code` prop as plain text (no Shiki highlighting — keep it simple)

**3. FeatureCard.astro** (UI-SPEC component inventory):
- Props: `icon: string; title: string; description: string`
- Container: `bg-card border border-border rounded-lg p-6 transition-colors duration-150 hover:border-muted-foreground`
- Icon: Render a Lucide icon name dynamically. Use an approach that maps icon names to SVG or accepts raw SVG as slot content. Simplest: accept a slot named "icon" for the Lucide icon SVG, or render a colored circle placeholder with the icon name.
  - Recommended: Accept a default `<slot />` for the icon content. The parent (FeatureGrid or page) passes in the Lucide icon as `<Icon class="size-6 text-primary" />` from `lucide-react` or as inline SVG. Icon wrapper: `class="text-primary"` with `aria-hidden="true"`.
- Title: `text-base font-bold` (16px bold per UI-SPEC)
- Description: `text-sm text-muted-foreground` (14px per UI-SPEC)

**4. FeatureGrid.astro** (UI-SPEC layout):
- Props: `sectionTitle?: string`
- If sectionTitle provided, render `<h2 class="text-2xl font-bold mb-6">{sectionTitle}</h2>`
- Grid container: `grid grid-cols-1 md:grid-cols-2 gap-6`
- Content via `<slot />` — parent passes FeatureCard components

**5. ProductHeader.astro** (UI-SPEC Enzo-Abyss layout, D-02):
- Props: `name: string; tagline: string`
- Name: `<h1 class="text-2xl font-bold">{name}</h1>` (Heading size 24px bold per D-02, simple header)
- Tagline: `<p class="text-base text-muted-foreground mt-2">{tagline}</p>`
- Container: `mb-12` for spacing below

**6. DeviceMockupPlaceholder.astro** (UI-SPEC interaction contract #3 of ReadeaHero):
- Props: `maxHeight?: string` (default "280px" mobile, use class approach)
- Container: `bg-muted rounded-2xl border border-border flex items-center justify-center`
- Height: `h-[280px] md:h-[320px] lg:h-[400px]` (responsive per UI-SPEC)
- Width: `w-full max-w-md mx-auto`
- Inner text: "Screenshot coming soon" in `text-muted-foreground text-sm`
- Alt-like attribute: add `role="img" aria-label="App screenshot placeholder"`

**7. ReadeaHero.astro** (UI-SPEC Readea landing layout):
- No props needed (self-contained hero)
- Full-width container (will be placed in HomeLayout `slot="hero"`)
- Background: CSS gradient `linear-gradient(to bottom, rgba(10, 14, 26, 0.7), rgba(10, 14, 26, 0.95))` over a subtle background color `bg-[#0a0e1a]`
- Centered content with max-width, vertical padding `py-20 md:py-24`
- Headline: "Readea" at `text-4xl font-bold text-[#e2e8f0]` (Display size, always light text)
- Tagline: "A modern reading experience for arXiv papers on macOS and iOS" at `text-base text-[#94a3b8]` (per D-10, always light)
- Import and render DeviceMockupPlaceholder below tagline
- CTA button: `<a href="/contact" class="inline-flex items-center justify-center px-6 py-3 bg-primary text-primary-foreground font-medium rounded-lg hover:opacity-90 transition-opacity">Contact for Beta Access</a>` (per D-12)
- All text uses hardcoded light colors (not theme tokens) so it stays light in both themes (per UI-SPEC)

**8. TutorialStep.astro** (UI-SPEC tutorial layout):
- Props: `stepNumber: number; title: string; description: string; hasScreenshot?: boolean`
- Step number: Badge-like circle `bg-primary text-primary-foreground w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold`
- Title: `text-base font-bold`
- Description: `text-base text-foreground` (body text)
- If hasScreenshot, render DeviceMockupPlaceholder with smaller height `h-[240px]`
- Spacing: `space-y-3` between elements within step

**9. TutorialNav.astro** (UI-SPEC interaction contract #1 TutorialNav):
- Props: `prev?: { title: string; href: string }; next?: { title: string; href: string }`
- Container: `flex justify-between items-center mt-12 pt-6 border-t border-border`
- Previous link (if prev): `<a href={prev.href} class="text-primary hover:underline transition-all duration-150 flex items-center gap-1">` with text "Previous: {title}" and Lucide ChevronLeft as inline SVG (6x6)
- Next link (if next): same pattern with ChevronRight, `ml-auto` to push right
- If only one direction exists, the other side is empty
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 9 component files exist in `src/components/`
    - `Breadcrumb.astro` contains `aria-label="Breadcrumb"` and `aria-current="page"`
    - `CodeBlock.astro` contains `aria-label="Copy code to clipboard"` and `navigator.clipboard`
    - `FeatureCard.astro` contains `hover:border-muted-foreground` and `aria-hidden`
    - `FeatureGrid.astro` contains `grid-cols-1 md:grid-cols-2`
    - `ProductHeader.astro` contains `text-2xl font-bold`
    - `DeviceMockupPlaceholder.astro` contains `Screenshot coming soon`
    - `ReadeaHero.astro` contains `Contact for Beta Access` and `href="/contact"` and `#e2e8f0` and `#94a3b8`
    - `TutorialStep.astro` contains `rounded-full` (step number badge)
    - `TutorialNav.astro` contains `Previous:` and `Next:`
    - Build succeeds
  </acceptance_criteria>
  <done>All 9 shared Astro components created per UI-SPEC, build passes, components ready for consumption by Plans 02-05</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time only | All components are static Astro — no user input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | CodeBlock copy button | accept | Copy button reads from static code prop, not user input; no injection risk |
| T-03-02 | Information Disclosure | SEOHead og:image | accept | OG images are intentionally public; no sensitive data in meta tags |
</threat_model>

<verification>
1. `npm run build` passes with no errors
2. All 9 component files exist under `src/components/`
3. Both `BaseLayout.astro` and `HomeLayout.astro` import and render `SEOHead`
4. `astro.config.mjs` includes `pagefind()` integration
5. `src/components/ui/select.tsx` and `src/components/ui/dialog.tsx` exist
</verification>

<success_criteria>
- SEOHead replaces BaseHead in both layouts — all existing pages automatically get OG/Twitter meta tags
- All shared components (Breadcrumb, CodeBlock, FeatureCard, FeatureGrid, ProductHeader, DeviceMockupPlaceholder, ReadeaHero, TutorialStep, TutorialNav) are created and buildable
- astro-pagefind is installed and configured in astro.config.mjs
- shadcn Select and Dialog components are available
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-pages-seo/03-01-SUMMARY.md`
</output>
