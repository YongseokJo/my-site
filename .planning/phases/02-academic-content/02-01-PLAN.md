---
phase: 02-academic-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/label.tsx
  - src/components/ui/separator.tsx
  - src/layouts/HomeLayout.astro
  - src/components/CosmicHero.astro
  - src/components/CTACardPair.astro
  - src/components/BioSummary.astro
  - src/pages/index.astro
autonomous: true
requirements:
  - PROF-01
must_haves:
  truths:
    - "Homepage displays a full-width cosmic hero with deep space background image and dark gradient overlay"
    - "Hero headline reads 'First Star Clusters & Black Holes in the Early Universe'"
    - "Two CTA cards appear below hero — Simulation Package and ArXiv App with buttons"
    - "Brief bio summary with 'Learn more' link to /about appears before footer"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "shadcn Card component"
    - path: "src/layouts/HomeLayout.astro"
      provides: "Full-width hero layout extending BaseLayout pattern"
    - path: "src/components/CosmicHero.astro"
      provides: "Full-width hero with bg.jpg overlay and headline"
    - path: "src/components/CTACardPair.astro"
      provides: "Two side-by-side CTA cards for sim package and arXiv app"
    - path: "src/components/BioSummary.astro"
      provides: "Brief bio with Learn more link"
    - path: "src/pages/index.astro"
      provides: "Homepage using HomeLayout with all three sections"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/HomeLayout.astro"
      via: "import and layout wrapper"
      pattern: "import HomeLayout"
    - from: "src/components/CosmicHero.astro"
      to: "public/images/bg.jpg"
      via: "background-image CSS"
      pattern: "bg\\.jpg"
---

<objective>
Install all Phase 2 shadcn UI components, create the HomeLayout for full-width hero support, and build the homepage with cosmic hero, CTA cards, and bio summary.

Purpose: The homepage is the first impression — it must convey the research theme (first star clusters and black holes) with atmospheric design, provide clear paths to the simulation package and arXiv app, and link to the About page.

Output: Six new shadcn UI components installed, HomeLayout.astro, CosmicHero.astro, CTACardPair.astro, BioSummary.astro, and a fully rebuilt index.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-academic-content/02-CONTEXT.md
@.planning/phases/02-academic-content/02-RESEARCH.md
@.planning/phases/02-academic-content/02-UI-SPEC.md
@src/layouts/BaseLayout.astro
@src/styles/global.css
@src/components/BaseHead.astro
@src/components/Navigation.astro
@src/components/Footer.astro

<interfaces>
<!-- From src/layouts/BaseLayout.astro -->
interface Props {
  title: string;
  description?: string;
}
<!-- Body classes: bg-background text-foreground min-h-screen flex flex-col font-sans -->
<!-- Main wrapper: flex-1 w-full max-w-[700px] mx-auto px-4 md:px-6 lg:px-8 py-16 -->

<!-- From src/components/ui/button.tsx — already installed -->
<!-- shadcn Button with variant="default"|"outline"|"ghost" and size="default"|"sm"|"lg" -->

<!-- Theme tokens from global.css -->
<!-- Dark: --background:#0a0e1a --foreground:#e2e8f0 --card:#141b2d --primary:#d4a54a --muted:#1a2236 --muted-foreground:#94a3b8 --border:#1e293b -->
<!-- Light: --background:#ffffff --foreground:#0f172a --card:#f8fafc --primary:#b8860b --muted:#f1f5f9 --muted-foreground:#64748b --border:#e2e8f0 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and create HomeLayout with CosmicHero</name>
  <files>
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
    src/components/ui/input.tsx
    src/components/ui/textarea.tsx
    src/components/ui/label.tsx
    src/components/ui/separator.tsx
    src/layouts/HomeLayout.astro
    src/components/CosmicHero.astro
  </files>
  <read_first>
    src/layouts/BaseLayout.astro
    src/components/BaseHead.astro
    src/components/ThemeScript.astro
    src/components/Navigation.astro
    src/components/Footer.astro
    src/styles/global.css
    .planning/phases/02-academic-content/02-UI-SPEC.md
  </read_first>
  <action>
1. Install all Phase 2 shadcn components in one command:
   ```bash
   npx shadcn@latest add card badge input textarea label separator
   ```
   This creates files in `src/components/ui/`. Do NOT modify these generated files.

2. Create `src/layouts/HomeLayout.astro` — a variant of BaseLayout that supports a full-width hero section OUTSIDE the 700px container. Structure (per D-01 and research Pattern 2):
   - Import global.css, BaseHead, ThemeScript, Navigation, Footer (same as BaseLayout)
   - Accept Props: `{ title: string; description?: string }`
   - HTML structure:
     ```
     html.dark > head(BaseHead + ThemeScript) > body(bg-background text-foreground min-h-screen flex flex-col font-sans)
       Navigation
       main.flex-1.w-full
         slot name="hero"    <-- full viewport width, no container constraint
         div.max-w-[700px].mx-auto.px-4.md:px-6.lg:px-8.py-16
           slot (default)    <-- 700px constrained content
       Footer
     ```
   The key difference from BaseLayout: `<main>` does NOT have max-width. The hero slot is full-width; the default slot is wrapped in the 700px container.

3. Create `src/components/CosmicHero.astro` — the full-width cosmic hero section (per D-01, D-02, UI-SPEC):
   - Full viewport width, NO max-width constraint
   - Background: `url('/images/bg.jpg')` with `background-size: cover`, `background-position: center`
   - CSS fallback background-color: `#0a0e1a` (so hero is never blank during image load)
   - Gradient overlay on top of image:
     - Dark mode: `linear-gradient(to bottom, rgba(10, 14, 26, 0.7), rgba(10, 14, 26, 0.95))`
     - Light mode: same overlay — hero is ALWAYS dark regardless of theme (per UI-SPEC)
   - Use a single overlay that works in both modes: `linear-gradient(to bottom, rgba(10, 14, 26, 0.7), rgba(10, 14, 26, 0.95))` applied via a pseudo-element or inner div with absolute positioning
   - Content centered, max-width 600px for headline:
     - Headline: "First Star Clusters & Black Holes in the Early Universe" — 36px (text-4xl on mobile scaling down is fine, but target Display size), font-bold, color `#e2e8f0` (always light, not theme-dependent)
     - Subtitle: "Computational Astrophysicist & ML Researcher" — 16px, font-normal, color `#94a3b8` (always muted light)
   - Padding: 48px vertical mobile (py-12), 64px vertical desktop (md:py-16), with 80px top on desktop (md:pt-20) per UI-SPEC spacing exception
   - Image tag: use CSS background-image (not img tag) with `loading` not applicable, but ensure the CSS references /images/bg.jpg
   - No CTA buttons in the hero itself (per UI-SPEC layout)
   - No JavaScript — purely static Astro component
  </action>
  <verify>
    <automated>cd /Users/yongseokjo/projects/my-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/card.tsx` exists and exports Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
    - File `src/components/ui/badge.tsx` exists and exports Badge
    - File `src/components/ui/input.tsx` exists and exports Input
    - File `src/components/ui/textarea.tsx` exists and exports Textarea
    - File `src/components/ui/label.tsx` exists and exports Label
    - File `src/layouts/HomeLayout.astro` contains `slot name="hero"` and `max-w-[700px]`
    - File `src/components/CosmicHero.astro` contains "First Star Clusters" and "bg.jpg" and "rgba(10, 14, 26"
    - `npm run build` succeeds with zero errors
  </acceptance_criteria>
  <done>All 6 shadcn components installed, HomeLayout supports full-width hero slot, CosmicHero renders headline with cosmic background overlay</done>
</task>

<task type="auto">
  <name>Task 2: Build CTACardPair, BioSummary, and wire homepage</name>
  <files>
    src/components/CTACardPair.astro
    src/components/BioSummary.astro
    src/pages/index.astro
  </files>
  <read_first>
    src/layouts/HomeLayout.astro
    src/components/CosmicHero.astro
    src/components/ui/card.tsx
    src/components/ui/button.tsx
    src/content/pages/about.md
    .planning/phases/02-academic-content/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `src/components/CTACardPair.astro` (per D-03, UI-SPEC):
   - Two side-by-side cards in a 2-column grid (gap-8 / 32px) that stack to single column on mobile (<768px)
   - Use `grid grid-cols-1 md:grid-cols-2 gap-8`
   - Each card uses the shadcn Card component (import from "../components/ui/card"):
     - Left card:
       - Lucide `Telescope` icon (import from lucide-react, render as static SVG or use the react component inline). Since this is Astro, use an inline SVG for the telescope icon OR import the icon as a React component. Simplest: use inline SVG matching Lucide's telescope path.
       - Title: "Simulation Package" (16px bold)
       - Description: "Explore cosmological simulation tools for galaxy formation and star cluster evolution." (16px regular, muted-foreground)
       - Button: "Explore Simulations" linking to `/software` — use the shadcn Button with variant default (accent bg). Wrap in an `<a>` tag.
     - Right card:
       - Lucide `Newspaper` icon (or `FileText` — choose an appropriate icon for an arXiv reading app)
       - Title: "ArXiv App"
       - Description: "Browse and organize arXiv papers with an intuitive reading experience."
       - Button: "Try ArXiv App" linking to `/arxiv-app`
   - Card styling: bg-card, border border-border, rounded-lg (radius-lg), padding 24px (p-6)
   - Button styling: accent background with primary-foreground text — use `className="bg-primary text-primary-foreground hover:bg-primary/90"`
   - Since shadcn Card is a React component and this is an Astro file, import Card components and use them directly (Astro can render React components without client directives for static rendering).

2. Create `src/components/BioSummary.astro` (per D-04, UI-SPEC):
   - Brief 2-3 sentence bio: "Yongseok Jo is a computational astrophysicist and ML researcher at the SkAI Institute in Chicago, studying the formation of first star clusters and black holes in the early universe through cosmological simulations."
   - "Learn more" link to `/about` using accent color: `text-primary hover:underline`
   - Visually separated from CTA cards by 48px (mt-12)
   - Text centered, muted-foreground for the bio, with the "Learn more" link on a new line below

3. Rewrite `src/pages/index.astro` (per D-01 through D-04):
   - Import HomeLayout (not BaseLayout)
   - Import CosmicHero, CTACardPair, BioSummary
   - Structure:
     ```astro
     <HomeLayout title="Yongseok Jo" description="Computational Astrophysicist researching first star clusters and black holes in the early universe">
       <CosmicHero slot="hero" />
       <CTACardPair />
       <BioSummary />
     </HomeLayout>
     ```
   - Title prop: "Yongseok Jo" (brand, not "Home")
   - Description: SEO-friendly research summary
  </action>
  <verify>
    <automated>cd /Users/yongseokjo/projects/my-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/CTACardPair.astro` contains "Simulation Package" and "ArXiv App" and "Explore Simulations" and "Try ArXiv App"
    - File `src/components/CTACardPair.astro` contains links to "/software" and "/arxiv-app"
    - File `src/components/BioSummary.astro` contains "Learn more" and 'href="/about"'
    - File `src/pages/index.astro` imports HomeLayout and contains `slot="hero"`
    - File `src/pages/index.astro` does NOT import BaseLayout
    - `npm run build` succeeds with zero errors
  </acceptance_criteria>
  <done>Homepage renders cosmic hero (full-width), two CTA cards (sim package + arXiv app), and bio summary with Learn more link. All per D-01 through D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static content only | Homepage is entirely static — no user input, no dynamic data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | CosmicHero background image | accept | Public image, no sensitive content. bg.jpg is a stock cosmic image. |
</threat_model>

<verification>
- `npm run build` completes without errors
- Built HTML at `dist/index.html` contains "First Star Clusters" headline
- Built HTML contains "Simulation Package" and "ArXiv App" CTA cards
- Built HTML contains "Learn more" bio link to /about
- Hero section is full-viewport-width (no max-w-[700px] constraint on hero)
- CTA cards and bio are within 700px container
</verification>

<success_criteria>
Homepage displays: (1) full-width cosmic hero with bg.jpg background, dark overlay, and research headline, (2) two CTA cards for simulation package and arXiv app below hero in 700px container, (3) bio summary with Learn more link to About page. All shadcn UI components needed for Phase 2 are installed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-academic-content/02-01-SUMMARY.md`
</output>
