---
phase: 02-academic-content
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/data/research-projects.ts
  - src/islands/ResearchProjects.tsx
  - src/pages/research.astro
autonomous: true
requirements:
  - RSRCH-01
  - RSRCH-02
  - RSRCH-03
must_haves:
  truths:
    - "Research page displays 2-3 curated project cards with icons, titles, and brief descriptions"
    - "Clicking a card expands it in-place to show full description, related publications, and status"
    - "Projects cover both scientific astrophysics and machine learning work"
    - "Only one project can be expanded at a time"
  artifacts:
    - path: "src/data/research-projects.ts"
      provides: "Curated research project data with titles, descriptions, related publications, and status"
      exports: ["researchProjects", "ResearchProject"]
    - path: "src/islands/ResearchProjects.tsx"
      provides: "React island with expand-in-place project cards"
      exports: ["default"]
    - path: "src/pages/research.astro"
      provides: "Research page route at /research"
  key_links:
    - from: "src/pages/research.astro"
      to: "src/islands/ResearchProjects.tsx"
      via: "client:load directive"
      pattern: "client:load"
    - from: "src/islands/ResearchProjects.tsx"
      to: "src/data/research-projects.ts"
      via: "import researchProjects"
      pattern: "import.*research-projects"
---

<objective>
Build the research projects page with curated project cards that expand in-place to show full descriptions, related publications, and status badges.

Purpose: Research projects contextualize Yongseok's work beyond individual papers — showing the big picture of both astrophysics simulations and ML applications for cosmology.

Output: research-projects.ts data file, ResearchProjects.tsx React island, rebuilt research.astro page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-academic-content/02-CONTEXT.md
@.planning/phases/02-academic-content/02-RESEARCH.md
@.planning/phases/02-academic-content/02-UI-SPEC.md
@src/data/publications.yaml
@src/layouts/BaseLayout.astro

<interfaces>
<!-- From src/components/ui/card.tsx (installed in Plan 01) -->
export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent }

<!-- From src/components/ui/badge.tsx (installed in Plan 01) -->
export { Badge, badgeVariants }

<!-- Publication IDs from publications.yaml for cross-referencing -->
<!-- first-author: jo-2025-toward, jo-2024-significance, jo-2024-evolution, jo-2023-calibrating, jo-2019-machineassisted -->
<!-- co-author: lee-2024-inferring, ni-2023-camels, villaescusanavarro-2023-camels, villaescusanavarro-2022-camels, shin-2020-dark, kim-2019-highredshift -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create research project data and ResearchProjects island</name>
  <files>
    src/data/research-projects.ts
    src/islands/ResearchProjects.tsx
  </files>
  <read_first>
    src/data/publications.yaml
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
    .planning/phases/02-academic-content/02-UI-SPEC.md
    .planning/phases/02-academic-content/02-RESEARCH.md
  </read_first>
  <action>
1. Create `src/data/research-projects.ts` (per D-08, D-09, D-10, D-11):

```typescript
export interface ResearchProject {
  id: string;
  title: string;
  brief: string;
  description: string;
  status: "Active" | "Completed";
  icon: string; // Lucide icon name
  relatedPublications: { title: string; year: number; url: string }[];
}

export const researchProjects: ResearchProject[] = [
  {
    id: "star-clusters",
    title: "First Star Clusters & Black Holes",
    brief: "Modeling the formation of the first star clusters and massive black holes in the early universe using self-consistent hybrid hydro/N-body cosmological simulations.",
    description: "This research program uses state-of-the-art cosmological simulations to understand how the first star clusters and black holes formed in the early universe. By developing self-consistent hybrid hydro/N-body methods, we can track the co-evolution of star clusters within their host galaxies across cosmic time. This work bridges the gap between small-scale star formation physics and large-scale cosmological structure formation, providing insights into the seeds of today's galaxy populations.",
    status: "Active",
    icon: "Sparkles",
    relatedPublications: [
      { title: "Evolution of Star Clusters within Galaxies Using Self-consistent Hybrid Hydro/N-body Simulations", year: 2024, url: "https://arxiv.org/abs/2408.03128" },
      { title: "High-redshift Galaxy Formation with Self-consistently Modeled Stars and Massive Black Holes", year: 2019, url: "https://arxiv.org/abs/1910.12888" },
    ],
  },
  {
    id: "ml-cosmology",
    title: "Machine Learning for Cosmological Simulations",
    brief: "Applying machine learning techniques to calibrate cosmological simulation parameters and estimate galactic baryonic properties across multiple simulation models.",
    description: "This research leverages machine learning to tackle fundamental challenges in computational cosmology. We develop methods to calibrate simulation parameters using implicit likelihood inference, test the robustness of ML models across different cosmological simulation suites (IllustrisTNG, SIMBA, ASTRID, SWIFT-EAGLE), and pioneer techniques for estimating galactic baryonic properties from dark matter halos. This work is conducted within the CAMELS collaboration, advancing our ability to constrain cosmological models with observational data.",
    status: "Active",
    icon: "Brain",
    relatedPublications: [
      { title: "Toward Robustness across Cosmological Simulation Models", year: 2025, url: "https://arxiv.org/abs/2502.13239" },
      { title: "Calibrating Cosmological Simulations with Implicit Likelihood Inference", year: 2023, url: "https://arxiv.org/abs/2211.16461" },
      { title: "Machine-assisted semi-simulation model (MSSM)", year: 2019, url: "https://arxiv.org/abs/1908.09844" },
    ],
  },
  {
    id: "galaxy-statistics",
    title: "Statistical Methods for Galaxy Observables",
    brief: "Developing statistical frameworks to constrain theoretical models from galaxy observables, with focus on covariance analysis and domain generalization.",
    description: "This line of research develops rigorous statistical methods for comparing cosmological simulations with observed galaxy populations. We investigate the significance of covariance structures in constraining theoretical models and develop domain-generalized neural networks that can transfer knowledge from simulations to real survey data like SDSS. These methods are essential for extracting maximum scientific insight from both current and upcoming galaxy surveys.",
    status: "Active",
    icon: "BarChart3",
    relatedPublications: [
      { title: "On the Significance of Covariance for Constraining Theoretical Models From Galaxy Observables", year: 2024, url: "https://arxiv.org/abs/2410.21722" },
      { title: "Inferring Cosmological Parameters on SDSS via Domain-generalized Neural Networks", year: 2024, url: "https://arxiv.org/abs/2409.02256" },
    ],
  },
];
```

The three projects cover: (1) astrophysics simulations (star clusters/black holes), (2) ML for cosmology (CAMELS), (3) statistical methods — satisfying D-11 (scientific + ML) and D-08 (2-3 curated projects). Descriptions are derived from the publication titles and abstracts in publications.yaml.

2. Create `src/islands/ResearchProjects.tsx` (per D-09, D-10, UI-SPEC):

**Component structure:**
- Import React useState
- Import Card, CardContent from shadcn
- Import Badge from shadcn
- Import Lucide icons: `Sparkles`, `Brain`, `BarChart3`, `ChevronDown`, `ChevronUp`, `ExternalLink`
- Import `researchProjects` from `../data/research-projects`

**Props:** None needed — data imported directly (only 3 projects, no build-time data needed).

**State:**
```typescript
const [expandedId, setExpandedId] = useState<string | null>(null);
```
Only one project expanded at a time (per UI-SPEC interaction contract).

**Render each project card:**
- Use shadcn Card component
- Card is clickable — `onClick` toggles expandedId (if same, collapse; if different, expand new)
- `cursor-pointer` class on the Card
- Card header area (always visible):
  - Lucide icon (48x48 area, use `size={32}` with padding) — map icon name string to component:
    ```typescript
    const iconMap: Record<string, React.ComponentType<any>> = { Sparkles, Brain, BarChart3 };
    const IconComponent = iconMap[project.icon];
    ```
  - Title: `text-base font-bold text-foreground`
  - Brief: `text-sm text-muted-foreground line-clamp-2` (2-3 lines max when collapsed)
  - Status badge: Badge with `variant="default"` for "Active" (accent bg), `variant="secondary"` for "Completed"
  - Chevron icon: `ChevronDown` when collapsed, `ChevronUp` when expanded — `text-muted-foreground` with `transition-transform duration-200`

**Expanded content (per D-10, RSRCH-02):**
- Use CSS `grid-template-rows: 0fr / 1fr` animation (per research code example):
  ```tsx
  <div style={{
    display: "grid",
    gridTemplateRows: isExpanded ? "1fr" : "0fr",
    transition: "grid-template-rows 200ms ease",
  }}>
    <div style={{ overflow: "hidden" }}>
      {/* expanded content */}
    </div>
  </div>
  ```
- Full description: `text-base text-foreground` multi-paragraph
- Related publications mini-list:
  - Heading: `text-sm font-bold text-foreground mb-2` — "Related Publications"
  - Each pub: `<a>` with title + year, linking to arXiv URL, with ExternalLink icon
  - Links open in new tab with `rel="noopener noreferrer"`
- Status + "Active" / "Completed" text repeated in expanded view is optional (already visible in collapsed card via badge)

**Layout:**
- Cards in a vertical list with `space-y-6` (24px gap per UI-SPEC)
- `max-w-full` — the page container (BaseLayout) already handles 700px max
- `aria-expanded` attribute on each card for accessibility

Export default function `ResearchProjects`.
  </action>
  <verify>
    <automated>cd /Users/yongseokjo/projects/my-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/data/research-projects.ts` exports `researchProjects` array with 3 objects
    - Each project has id, title, brief, description, status, icon, relatedPublications fields
    - At least one project has icon "Sparkles" (astrophysics) and one has icon "Brain" (ML)
    - File `src/islands/ResearchProjects.tsx` exists and exports default function
    - Contains `expandedId` state with useState
    - Contains `gridTemplateRows` for expand animation
    - Contains `aria-expanded` attribute
    - Contains Card, Badge imports from shadcn
    - Contains ChevronDown and ChevronUp imports from lucide-react
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>ResearchProjects island renders 3 curated project cards with expand-in-place behavior, 200ms animation, status badges, and related publication links.</done>
</task>

<task type="auto">
  <name>Task 2: Wire research page with ResearchProjects island</name>
  <files>
    src/pages/research.astro
  </files>
  <read_first>
    src/islands/ResearchProjects.tsx
    src/pages/research.astro
    src/layouts/BaseLayout.astro
  </read_first>
  <action>
Rewrite `src/pages/research.astro` to use the ResearchProjects island. The Astro file should:

- In the frontmatter section (between Astro's fence delimiters), import BaseLayout from "../layouts/BaseLayout.astro" and import ResearchProjects from "../islands/ResearchProjects.tsx"
- In the template section, wrap everything in BaseLayout with title="Research" and description="Research projects by Yongseok Jo -- star cluster formation, machine learning for cosmology, and statistical methods for galaxy observables"
- Add `<h1 class="text-2xl font-bold mb-8">Research</h1>` as the page heading
- Render `<ResearchProjects client:load />` below the heading

Key details:
- Use BaseLayout (not HomeLayout) — research page uses standard 700px container
- `client:load` because the expand-in-place interaction needs immediate JS hydration
- Page title "Research" at Heading size (24px bold) per UI-SPEC
- Description prop for SEO
  </action>
  <verify>
    <automated>cd /Users/yongseokjo/projects/my-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pages/research.astro` imports ResearchProjects from "../islands/ResearchProjects.tsx"
    - Contains `client:load` directive
    - Contains `<h1` with "Research"
    - Uses BaseLayout (not HomeLayout)
    - `npm run build` succeeds and generates `dist/research/index.html`
  </acceptance_criteria>
  <done>Research page at /research displays 3 curated project cards with expand-in-place interaction. Covers both scientific and ML projects per D-11 and RSRCH-01 through RSRCH-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static data only | Research project data is hardcoded in TypeScript, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | External publication links in related pubs | accept | All URLs are hardcoded arXiv links controlled by site owner. Open in new tab with rel="noopener noreferrer". |
</threat_model>

<verification>
- `npm run build` completes without errors
- Built HTML at `dist/research/index.html` exists
- ResearchProjects island hydrates with `client:load`
- Three project cards render with correct titles
</verification>

<success_criteria>
Research page displays 3 curated project cards (star clusters, ML for cosmology, statistical methods). Clicking a card expands it in-place with smooth animation to show full description, related publications with links, and status. Only one card expanded at a time. Both scientific and ML work represented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-academic-content/02-03-SUMMARY.md`
</output>
