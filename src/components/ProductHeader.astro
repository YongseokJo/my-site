---
interface Props {
  name: string;
  tagline: string;
}

const { name, tagline } = Astro.props;
---
<div class="mb-12">
  <h1 class="text-2xl font-bold">{name}</h1>
  <p class="text-base text-muted-foreground mt-2">{tagline}</p>
</div>
